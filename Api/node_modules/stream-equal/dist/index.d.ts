import { Readable } from 'stream';
declare const _default: (stream1: Readable, stream2: Readable) => Promise<boolean>;
/**
 * Tests that two readable streams are equal.
 *
 * @param {Readable} stream1
 * @param {Readable} stream2
 * @returns {boolean}
 */
export = _default;
